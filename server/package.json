{
  "name": "proto-note",
  "version": "1.0.0",
  "description": "m1z0r3 CTF 2018",
  "author": "Koki Sato",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "format": "run-s format:*",
    "format:prettier": "prettier --config prettier.config.js --write 'src/**/*.ts{,x}'",
    "format:tslint": "tslint --fix -p . 'src/**/*.ts{,x}'",
    "lint": "tslint -p . 'src/**/*.ts{,x}'",
    "serve": "node dist/app.js",
    "watch": "run-p watch:*",
    "watch:build": "env NODE_ENV=development tsc --watch",
    "watch:serve": "env NODE_ENV=development nodemon dist/app.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "mysql": "^2.16.0"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/mysql": "^2.15.5",
    "@types/node": "^10.12.1",
    "nodemon": "^1.18.5",
    "npm-run-all": "^4.1.3",
    "prettier": "^1.14.3",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typescript": "^3.1.4"
  }
}
